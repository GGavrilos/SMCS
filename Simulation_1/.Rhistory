sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
mu_new <- c(y_old, -y_old)
sd_new <- c(1 + sqrt(abs(y_old)), 1 + sqrt(abs(y_old)))
y_new = rnormMix(1, mean1 = mu_new[1], mean2 = mu_new[2],
sd1 = sd_new[1], sd2  = sd_new[2],
p.mix = p) # Draw a new outcome y. #
load("~/polybox - Georgios Gavrilopoulos (georgios.gavrilopoulos@stat.math.ethz.ch)@polybox.ethz.ch/PhD/Projects/SMCS/RCode/Simulation_1/SMSC, Cluster/smcs.rda")
load("~/polybox - Georgios Gavrilopoulos (georgios.gavrilopoulos@stat.math.ethz.ch)@polybox.ethz.ch/PhD/Projects/SMCS/RCode/Simulation_1/SMSC, Cluster/coverage.rda")
View(coverage)
plot(colMeans(coverage))
plot(colMeans(smcs))
plot(colMeans(smcs), type = "l")
plot(colMeans(coverage), type = "l")
View(smcs)
load("~/polybox - Georgios Gavrilopoulos (georgios.gavrilopoulos@stat.math.ethz.ch)@polybox.ethz.ch/PhD/Projects/SMCS/RCode/Simulation_1/SMSC, Cluster/smcs.rda")
load("~/polybox - Georgios Gavrilopoulos (georgios.gavrilopoulos@stat.math.ethz.ch)@polybox.ethz.ch/PhD/Projects/SMCS/RCode/Simulation_1/SMSC, Cluster/coverage.rda")
plot(colMeans(coverage))
plot(colMeans(coverage), type = "l")
a = c(1,2,3)
a[2:]
a[2:3] <- 0
